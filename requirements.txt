# Pipeline + test dependencies, pinned to the known-good environment.
# Frontend dependencies live in frontend/package.json.
#
# Install:  python -m pip install -r requirements.txt

# dbt transform layer (Postgres adapter)
dbt-core==1.11.9
dbt-postgres==1.11.0b1

# Dagster orchestration. The asset graph shells out to dbt via subprocess and
# uses Dagster's default local storage, so dagster-dbt and dagster-postgres are
# deliberately not pinned — nothing imports them.
dagster==1.13.4
dagster-webserver==1.13.4

# Postgres driver + YAML config
psycopg2-binary==2.9.12
PyYAML==6.0.3

# Test runner
pytest==8.4.2
